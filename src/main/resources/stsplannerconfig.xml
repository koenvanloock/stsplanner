<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <scanAnnotatedClasses>
        <packageInclude>company.tothepoint.stsplanner.model</packageInclude>
    </scanAnnotatedClasses>

    <!-- Score configuration -->
    <scoreDirectorFactory>
        <easyScoreCalculatorClass>company.tothepoint.stsplanner.calculator.ScoreCalculator</easyScoreCalculatorClass>
    </scoreDirectorFactory>

    <!-- Optimization algorithms configuration -->
    <termination>
        <secondsSpentLimit>5</secondsSpentLimit>
    </termination>

    <localSearch>
        <changeMoveSelector>
            <filterClass>company.tothepoint.stsplanner.filter.ParticipationSelectionMoveFilter</filterClass>
        </changeMoveSelector>
    </localSearch>

    <environmentMode>PRODUCTION</environmentMode>-->
</solver>